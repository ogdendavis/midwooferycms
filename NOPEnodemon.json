{
  "events": {
    "start": "echo 'Starting PostgreSQL server' && pg_ctl -D /usr/local/var/postgres start -w",
    "crash": "echo 'Stopping PostgreSQL server' && pg_ctl -D /usr/local/var/postgres stop -m smart",
    "exit": "Stopping PostgreSQL server' && pg_ctl -D /usr/local/var/postgres stop -m smart"
  }
}
